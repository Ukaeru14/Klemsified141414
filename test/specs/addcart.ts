import addcart from '../pageobjects/addtocart.ts';
 
describe('Add product to cart', () => {
    it('should be able to add products to cart', async () => {
       
    await addcart.secondtest();
    await addcart.thirdtest();
 
    });
});